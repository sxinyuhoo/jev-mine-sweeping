# TypeSafe（Jev）API key —— 与 config/config.yaml 里的 jev.api_key 等价，
# 环境变量优先。复制为 .env 后自行 source，或在 shell 里 export。
TYPESAFE_API_KEY=

# 可选覆盖
# JEV_MODEL=jev-latest
# JEV_ENDPOINT=https://api.typesafe.ai/v1/systemone
# JEV_BACKEND=http
# JEV_MS_CONFIG=/absolute/path/to/config.yaml
